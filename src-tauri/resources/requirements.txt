# Development/runtime dependencies for the local worker.
# The current machine already has these installed.  Pinning them here makes
# a future worker sidecar (Nuitka, see scripts/build-worker-exe.ps1)
# reproducible without any runtime downloads.
texteller
torch==2.13.0
torchvision==0.28.0
